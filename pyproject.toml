[tool.ruff]
target-version = 'py37'
line-length = 120
exclude = [
    "migrations",
    "__pycache__",
    "manage.py",
    "settings.py",
    "env",
    ".env",
    "venv",
    ".venv",
]

[tool.ruff.format]
docstring-code-format = true
quote-style = "single"
